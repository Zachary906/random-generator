window.exportUserData()           // Download data
window.undo()                     // Undo last change
window.redo()                     // Redo last change
window.displayStats()             // Show detailed stats
window.createManualBackup()       // Create backup now
window.setFilter('caught', true)  // Show only caught
window.clearFilters()             // Clear all filters